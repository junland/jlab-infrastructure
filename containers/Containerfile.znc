FROM docker.io/centos:latest

RUN dnf update -y && dnf install znc znc-modtcl znc-modpython -y

RUN dnf clean all

RUN mkdir -p /znc

WORKDIR /znc

EXPOSE 6697

USER znc

CMD ["znc", "--foreground", "--datadir", "/znc"]
